{
  "name": "tattantat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "cd web && npm install --prefer-offline --no-audit && cd ../backend && npm install --prefer-offline --no-audit",
    "build": "cd web && npm run build && cd ../backend && npm run build",
    "start": "node server.js",
    "start:web": "cd web && npm run start",
    "start:backend": "cd backend && npm run start:prod"
  },
  "dependencies": {
    "lucide-react": "^1.47.0"
  }
}
