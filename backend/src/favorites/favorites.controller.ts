import { Controller, Delete, Get, Param, Post, Req, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { FavoritesService } from './favorites.service';
@Controller('favorites') @UseGuards(JwtAuthGuard) export class FavoritesController { constructor(private readonly service: FavoritesService) {} @Get() list(@Req() r:{user:{id:string}}){return this.service.list(r.user.id)} @Post(':productId') add(@Req() r:{user:{id:string}},@Param('productId') id:string){return this.service.add(r.user.id,id)} @Delete(':productId') remove(@Req() r:{user:{id:string}},@Param('productId') id:string){return this.service.remove(r.user.id,id)} }
