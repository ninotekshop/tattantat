require('dotenv').config({quiet:true});const{Client}=require('pg');const rows=[['FREE','Free',0,10],['PRO','Pro',99000,50],['BUSINESS','Business',299000,200],['ENTERPRISE','Enterprise',499000,null]];const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});c.connect().then(async()=>{for(const r of rows)await c.query("INSERT INTO subscription_plans(code,name,price,billing_cycle,max_listings)VALUES($1,$2,$3,'MONTHLY',$4) ON CONFLICT(code) DO NOTHING",r);console.log('SUBSCRIPTION_PLANS_SEEDED')}).finally(()=>c.end());
